import React, { PureComponent } from 'react'

import "./Navbar.scss"

class Navbar extends PureComponent {
    constructor(props) {
        super(props)

        this.state = {
            
        }
    }

    render() {
        return (
            <div>Navbar</div>
        )
    }
}

export default Navbar