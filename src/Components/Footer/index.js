import React, { PureComponent } from 'react'

import "./Footer.scss"

class Footer extends PureComponent {
    constructor(props) {
        super(props)

        this.state = {
            
        }
    }

    render() {
        return (
            <div>Footer</div>
        )
    }
}

export default Footer